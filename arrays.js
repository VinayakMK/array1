/*const arr = require("readline-sync");

let array = arr.questionInt("Enter size of the Array :");
let string = arr.question("Enter Elements :");

for(i=0;i<array;i++)

    {
        arr[i]=arr.question();
        
    }


console.log(arr);*/

